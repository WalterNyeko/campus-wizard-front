import React from 'react';
import MainNav from '../../components/commons/MainNav';

const Register = ({ handleSubmit, handleInputChange }) => {
  return (
    <React.Fragment>
        <MainNav />
    </React.Fragment>
  )
}
export default Register;